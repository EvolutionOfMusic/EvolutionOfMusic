#***********************************************#
#* Stephen Andersen                    ECE 420 *#
#* 011363024                                   *#
#***********************************************#

CC = g++
INC_DIR = ../../../
CFLAGS = -g -I$(INC_DIR) #-Wall
DEPS = AGIbase.h $(INC_DIR1)/CogServer.h $(INC_DIR1)/Factory.h $(INC_DIR2)/Logger.h $(INC_DIR3)/SimpleTruthValue.h song_structs.h frequencies.h
OBJ = AGIbase.o CogServer.o 

%.0: %.cpp #$(DEPS)
	$(CC) $(CFLAGS) -c -o $@ $<

AGIbase: $(OBJ)
	g++ $(CFLAGS) -o $@ $^

clean: 
	rm -rf *.o

delete: clean
	rm -rf AGIbase
	rm -rf *~

help: 
	@echo 'RULES: AGIbase, clean, delete'
